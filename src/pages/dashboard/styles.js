import { makeStyles } from "@material-ui/styles";

export default makeStyles(() => ({
  tableWidget: {
    overflowX: "auto",
  },
  searchLog: {
    color: '#fff'
  },
  label: {
    color: '#fff !important'
  },
  input: {
    color: '#fff'
  }
}));
